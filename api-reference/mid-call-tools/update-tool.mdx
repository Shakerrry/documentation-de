---
title: "Mid-Call-Tool aktualisieren"
api: "PUT /user/tools/{id}"
description: "Bestehendes Mid-Call-Tool aktualisieren"
---

Dieser Endpunkt ermöglicht es Ihnen, ein bestehendes Mid-Call-Tool zu aktualisieren. Alle Felder sind optional - geben Sie nur die Felder an, die Sie aktualisieren möchten.

### Pfad-Parameter

<ParamField path="id" type="integer" required>
  Eindeutiger Bezeichner des zu aktualisierenden Tools
</ParamField>

### Anfragekörper-Parameter

<ParamField body="name" type="string" optional>
  Tool-Name - darf nur Kleinbuchstaben und Unterstriche enthalten und muss mit einem Buchstaben beginnen
</ParamField>

<ParamField body="description" type="string" optional>
  Detaillierte Erklärung, wann und wie die KI dieses Tool verwenden soll (max. 255 Zeichen)
</ParamField>

<ParamField body="endpoint" type="string" optional>
  Gültige URL des aufzurufenden API-Endpunkts
</ParamField>

<ParamField body="method" type="string" optional>
  HTTP-Methode: `GET`, `POST`, `PUT`, `PATCH` oder `DELETE`
</ParamField>

<ParamField body="timeout" type="integer" optional>
  Anfrage-Timeout in Sekunden (1-30)
</ParamField>

<ParamField body="headers" type="array" optional>
  Mit der Anfrage zu sendende HTTP-Header (ersetzt vorhandene Header)
  <Expandable title="headers-Eigenschaften">
    <ParamField body="name" type="string" required>
      Header-Name
    </ParamField>
    <ParamField body="value" type="string" required>
      Header-Wert
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="schema" type="array" optional>
  Parameter-Schema (ersetzt vorhandenes Schema)
  <Expandable title="schema-Eigenschaften">
    <ParamField body="name" type="string" required>
      Parameter-Name (2-32 Zeichen)
    </ParamField>
    <ParamField body="type" type="string" required>
      Parameter-Typ: `string`, `number` oder `boolean`
    </ParamField>
    <ParamField body="description" type="string" required>
      Parameter-Beschreibung (3-255 Zeichen)
    </ParamField>
  </Expandable>
</ParamField>

### Antwortfelder

<ResponseField name="message" type="string">
  Erfolgsmeldung
</ResponseField>

<ResponseField name="data" type="object">
  Das aktualisierte Tool-Objekt mit allen aktuellen Werten
</ResponseField>

<ResponseExample>

```json 200 OK
{
  "message": "Tool erfolgreich aktualisiert",
  "data": {
    "id": 1,
    "name": "update_customer_info",
    "description": "Verwenden Sie dieses Tool, um Kundeninformationen im System zu aktualisieren.",
    "endpoint": "https://api.yourcompany.com/customers/update",
    "method": "POST",
    "timeout": 15,
    "headers": [
      {
        "name": "Content-Type",
        "value": "application/json"
      }
    ],
    "schema": [
      {
        "name": "customer_name",
        "type": "string",
        "description": "Vollständiger Name des Kunden"
      },
      {
        "name": "customer_age",
        "type": "number",
        "description": "Alter des Kunden"
      },
      {
        "name": "newsletter_subscription",
        "type": "boolean",
        "description": "Ob der Kunde den Newsletter abonnieren möchte"
      }
    ],
    "created_at": "2025-10-10T12:00:00.000000Z",
    "updated_at": "2025-10-10T14:30:00.000000Z"
  }
}
```

```json 404 Nicht gefunden
{
  "message": "Tool nicht gefunden"
}
```

```json 422 Validierungsfehler
{
  "message": "Validierung fehlgeschlagen",
  "errors": {
    "name": ["Tool-Name darf nur Kleinbuchstaben und Unterstriche enthalten und muss mit einem Buchstaben beginnen."]
  }
}
```

</ResponseExample>

### Tool-Zuweisungen verwalten

Um dieses Tool an Assistenten anzuhängen oder zu trennen, verwenden Sie die Assistant-API:

- **[Assistenten erstellen](/api-reference/assistants/create-assistant)** - Verwenden Sie den Parameter `tool_ids`, um Tools beim Erstellen eines Assistenten anzuhängen
- **[Assistenten aktualisieren](/api-reference/assistants/update-assistant)** - Verwenden Sie den Parameter `tool_ids`, um zu verwalten, welche Tools einem Assistenten zugewiesen sind
